version: 1
applications:
  - appRoot: frontend
    framework: React
    build:
      commands:
        - npm ci
        - npm run build
    artifacts:
      baseDirectory: frontend/dist
      files:
        - '**/*'
    cache:
      paths:
        - 'frontend/node_modules/**/*'
